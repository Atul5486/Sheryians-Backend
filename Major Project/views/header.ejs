<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Scatch - A permium Bag Shop</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    *{
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }
    html,body{
        width: 100%;
        height: 100%;
    }
  </style>
</head>
<body>
  <nav class="fixed top-0 left-0 w-full h-16 bg-zinc-800 flex justify-between items-center px-10 z-10 text-white">
    <h3 class="text-2xl font-bold">Scatch</h3>
    <% var loggedIn=typeof loggedIn !== 'undefined' ? loggedIn : true; %>
    <% if(loggedIn){ %>
        <div class="flex gap-5">
            <a href="/shop">Shop</a>
            <a href="/cart">Cart</a>
            <a href="/account">My Account</a>
            <a class="text-red-500" href="/users/logout">Logout</a>

        </div>
    <% } %>
  </nav>
</body>
</html>
