<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="./stylesheet/style.css">
</head>
<body>
    <div class="w-full h-full bg-zinc-900 p-10 text-white">
        <div class="nav">
            <a class="text-blue-500 text-xl" href="/">Back to home</a>
        </div>
        <h1 class="text-2xl mb-5">All User</h1>
        <div class="users flex gap-3 flex-warp">
            <% if(users.length >0){ %>
       <% users.forEach(function(user){ %>
                <div class="user w-60 p-2 bg-zinc-700 rounded-lg ">
            <div class="w-full h-60 bg-zinc-500 rounded-lg overflow-hidden">
                <img class="w-full h-full object-cover  hover:scale-125 transition-all duration-1000 cursor-pointer" src="<%=user.image%>" alt="">
            </div>
            <div class="text mt-3">
                <h3 class="text-xl tracking-tighter"><%= user.name%></h3>
                <h3 class="text-zinc-400  tracking-tighter"><%=user.email%></h3>
                <div class="flex justify-between mt-5">
                    <a class="text-zinc-400" href="/edit/<%=user._id%>">Edit User</a>
                    <a class="text-red-400"  href="/delete/<%=user._id%>">Delete  User</a>
                </div>
                </div>
            </div>
            <% }) %>
           <% }else{%>
            <p class="text-red-500 text-2xl">No user</p>
            <%} %>
        
    </div>
    </div>



    <script src="./javascript/script.js"></script>
</body>
</html>