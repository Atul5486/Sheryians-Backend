<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../stylesheet/style.css">
   <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
</head>
<body>
   <div class="w-full min-h-full bg-zinc-800 p-10">
    <div class="nav">
        <a class="text-blue-500 text-2xl " href="/">Back to Home</a>
        <h3 class="my-4 text-3xl">All User</h3>
    </div>
    <div class="users flex gap-3 flex-wrap">
        <% if(users.length > 0 ){ %>
        <% users.forEach(function(user){ %>
                <div class="user w-60 bg-zinc-700 px-2 py-2 rounded-lg">
            <div class="h-44 w-full bg-zinc-400 rounded-lg overflow-hidden">
                <img class="w-full h-full object-cover object-center  hover:object-top transition-all duration-200 ease" src="<%=user.image%>" alt="">
            </div>
            <div class="text my-2">
                <h1 class="text-2xl tracking-tighter"><%=user.name%></h1>
            <h1 class="text-zinc-400"><%=user.email%></h1>
            </div>
            <div class="btn flex justify-between px-2">
                <a class="text-blue-400" href="/edit/<%=user._id%>">edit</a>
                <a class="text-red-400" href="/delete/<%=user._id%>">delete</a>
            </div>
            
        </div>
          <%  })%>
        <%}else{%>
                <p class="text-red-600 text-3xl">No user Found</p>
           <% } %>
    </div>
</body>
</html>